
description: Route HTTP messages for virtual hosts
rules:
- description:
  pluck: host
  regex: ^localhost$
  route:
  - http.renderer.markdown.singleton
  - http.translator.file.singleton
  - server.fileServer.singleton
